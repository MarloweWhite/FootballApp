<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.c1733667.team10_football_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StadiumActivity" />
        <activity android:name=".ChampionshipLeague" />
        <activity android:name=".Score" />
        <activity android:name=".PremierLeague" />
        <activity android:name=".LeagueOne" />
        <activity android:name=".LeagueTwo" />
        <activity android:name=".InfoActivity"></activity>
    </application>

</manifest>